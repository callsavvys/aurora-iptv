{
  "name": "aurora-iptv-mac",
  "version": "0.2.1",
  "description": "Local-first IPTV player for macOS",
  "main": "main.cjs",
  "private": true,
  "scripts": {
    "start": "electron .",
    "package:mac": "electron-packager . 'Aurora IPTV' --platform=darwin --arch=arm64 --out=release --overwrite --asar --prune=true --app-bundle-id=com.aurora.iptv --app-version=0.2.1"
  },
  "dependencies": {
    "electron": "^37.6.0",
    "hls.js": "^1.6.13"
  },
  "devDependencies": {
    "@electron/packager": "^19.0.0"
  }
}
